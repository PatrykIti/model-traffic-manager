apiVersion: v1
kind: Service
metadata:
  name: router-shared-service-mock
spec:
  selector:
    app: router-shared-service-mock
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
